import React from 'react'

interface Props {
    id?: string;
};

const CustomerDetails = ({ id }: Props) => {
    return (
        <div>
            details page
        </div>
    )
}

export default CustomerDetails
